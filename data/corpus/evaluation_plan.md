# Evaluation Plan

The evaluation harness compares three chunking strategies and two retrieval modes.

The strategies are fixed, structural, and semantic. The retrieval modes are hybrid and dense-only.

Metrics include correctness, faithfulness, context relevance, and citation accuracy.

Cached response files are stored under `data/eval_cache` for reproducible offline runs.
